# Infrastructure layer specific variables for production environment

# This file contains infrastructure-specific overrides for production
# Global variables are inherited from global.tfvars

# Production-specific overrides can go here if needed
# Example: larger instances, enhanced monitoring, etc.
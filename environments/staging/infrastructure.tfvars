# Infrastructure layer specific variables for staging environment

# This file contains infrastructure-specific overrides for staging
# Global variables are inherited from global.tfvars

# Staging-specific overrides can go here if needed
# Example: smaller instances, different configurations, etc.
# Applications layer specific variables for staging environment

# This file contains application-specific overrides for staging
# Global variables are inherited from global.tfvars

# Staging-specific overrides can go here if needed
# Example: disable certain applications, use internal load balancers, etc.